# Project Handoff: mvegaslanding

## Purpose
Landing page project for M Vegas loan lead generation work.

## Start Here
- Check `package.json` for available commands.
- Review current deployment/config before changing lead capture or tracking behavior.
- Run `git status` before edits.

## Local/Private Files
- Do not commit `.env` or other local secrets.
- `.env.example` is safe to use as the setup reference.

## GitHub
- Branch: `main`
